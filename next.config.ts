import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["localhost", "plus.unsplash.com","trendai-backend-production-f6c2.up.railway.app"],
  },
};

export default nextConfig;
